# MovieLens 1M 数据治理项目交接说明

更新日期：2026-09-24。本文面向接手迭代一的同事，区分**已经实跑完成**与**尚待开发**的内容。项目总要求见仓库根目录的 `引言_项目总体要求与汇报安排.md`，本轮细则见 `迭代一_Hadoop数据清洗与Agent基础.md`。第一次汇报安排在 2026-09-30，建议优先完成迭代一的 Agent 接入和前端演示。

## 一、目前进度

| 原定五步 | 状态 | 已有成果 / 尚缺内容 |
| --- | --- | --- |
| 1. 梳理要求、登记数据与确定规则 | 已完成 | 原始数据清单、SHA-256、清洗及五维评分规则 v1.0.0、时间切分方法。 |
| 2. 搭建并验证 Hadoop 环境 | 已完成（本机） | WSL Ubuntu + Hadoop 3.5.0 单节点 HDFS/YARN，WordCount 烟雾测试通过。换机器需重新部署。 |
| 3. 实现并实跑 Hadoop 清洗与评分 | 已完成 | 清洗前评分 → 清洗 → 净表导出 → 清洗后评分，四个 MapReduce 作业均成功；报告、审计、数据版本和 `T1/T2` 已产生。 |
| 4. 封装 Agent 可调用任务与追问接口 | 未开始 | 目前只有命令行驱动脚本，尚无自然语言入口、服务端 API、任务队列或报告驱动的追问能力。 |
| 5. 前端、端到端验收与汇报材料 | 未开始 | 尚无页面或从一次用户提示到结果展示的完整演示；PPT/视频为可选。 |

GitHub 仓库：<https://github.com/NanjinLin/BigData-lab2>。第三步已上传至 `main` 的提交 [`da3ea29`](https://github.com/NanjinLin/BigData-lab2/commit/da3ea29fa50b7f0e1acf6edf1354e1f3020087de)。**本交接文档目前只在工作区，尚未提交或推送。**

## 二、已完成的实现

- `raw_data_manifest.json` 登记三个原始 `.dat` 文件的大小、行数和 SHA-256；驱动运行前逐文件核验。原始数据按 ISO-8859-1 解码，`::` 分隔，首行不是表头。
- `config/quality_rules_v1.json` 和 `docs/迭代一_清洗与评分规则_v1.md` 固定规则、计分口径和限制。清洗前后使用同一规则；Accurate/Complete/Unique/Consistent 先逐表评分再对三表等权平均，Up-to-date 只评价有时间戳的评分表。不设综合分。
- `hadoop_jobs/mapper.py`、`clean_reducer.py`、`score_reducer.py`、`quality.py` 实现 Hadoop Streaming 作业。清洗处理字段错误、无效评分/时间、跨表悬空引用、重复和冲突，并输出隔离、去重、置空及规范化审计。净表为 UTF-8 的 `C\t{JSON}` 带表名记录，**不是原始 `.dat` 格式**。
- `scripts/run_iteration1.py` 负责校验输入、上传 HDFS、串联四项 Hadoop 作业、下载结果及对账；清洗和五维评分计算实际在 Hadoop 中完成。每次任务有独立目录、状态文件、日志、报告与 HDFS 路径。失败时写明阶段和错误，不生成占位分数。
- `tests/test_hadoop_rules.py` 覆盖冲突、去重、时间切分和规范化审计的主要语义。当前单元测试通过。环境和实跑结果分别见 `docs/WSL_Hadoop_运行环境.md`、`docs/迭代一_Hadoop实跑结果.md`。

正式成功任务 ID：`20260923T144724Z-3509cc23`。其版本和结果如下；逐表分子/分母、原因统计、样例、Job ID、HDFS 路径都在 `runs/20260923T144724Z-3509cc23/report.json`。

| 五维得分（百分制） | 清洗前 | 清洗后 |
| --- | ---: | ---: |
| Accurate | 92.66 | 97.19 |
| Complete | 99.11 | 98.89 |
| Unique | 95.89 | 100.00 |
| Up-to-date | 1.88 | 2.18 |
| Consistent | 83.84 | 100.00 |

| 表 | 原始行 | 隔离行 | 去重行 | 净表行 |
| --- | ---: | ---: | ---: | ---: |
| ratings | 1,150,241 | 154,128 | 0 | 996,113 |
| users | 6,946 | 108 | 118 | 6,112 |
| movies | 4,465 | 70 | 204 | 3,941 |

数据版本：`raw-ml1m-r1b3c4690-uC0397B2E-mE256099E` → `clean-c91a5e196ca1af48c3da`；规则版本：`quality-clean-v1.0.0`。时间边界为 `T1=2000-12-01T23:59:59Z`、`T2=2000-12-28T23:59:59Z`；训练/验证/测试期分别有 797,116 / 100,886 / 98,111 条保留评分。后续迭代必须绑定同一净表版本和这组边界。

解释结果时注意：Accurate 是可检查值的合法性代理分，不能证明现实真实性；Complete 略降，是因为不确定的用户/电影属性被置为 `\N`；Up-to-date 使用数据发布时期的固定历史窗口，低分不意味着应删除旧评分。清洗后得分只评价保留集，**隔离不是修复**。

## 三、产物位置与运行方法

仓库保留了正式 `report.json` 和两份评分原始 TSV。完整净表 `cleaned.tsv` 约 141 MB、清洗/审计流 `clean_process.tsv` 约 174 MB，以及作业日志，仅在本机的 `runs/20260923T144724Z-3509cc23/` 与对应 HDFS 任务路径中，**未上传 GitHub**。同事若在其他机器克隆仓库，需从原机器转交这两份数据，或搭建环境后重新运行；不能把仓库里的报告误认为包含净表。较早的任务目录中有失败尝试和一次旧代码的成功尝试，正式结果只认上述任务 ID。

本机 Windows 工作区：`D:\大数据分析 26Fall\实验\实验二`；WSL 项目链接：`/home/linxinan/ml1m-project`。本机为 Ubuntu 24.04、Java 17、Hadoop 3.5.0；Hadoop 位于 `/opt/hadoop-3.5.0`。`scripts/run_iteration1.py` 将 Hadoop 路径、配置链接及 HDFS 根目录写为本机路径，迁移时需调整。NameNode 数据目录已初始化，**不要再次执行 `hdfs namenode -format`**。

截至交接检查，本机四个 Hadoop 服务均为 `inactive`。启动方式和健康检查命令见 `docs/WSL_Hadoop_运行环境.md`；运行期间还要保持一个 WSL 会话，避免 WSL 自动关闭。服务就绪后，在 Windows PowerShell 执行：

```powershell
wsl -d Ubuntu -- python3 /home/linxinan/ml1m-project/scripts/run_iteration1.py
```

脚本也接受 `--task-id`（1–80 位 ASCII 字母、数字、下划线或连字符）；每次应使用新 ID，不能覆盖已有目录。单次完整实跑在本机约 9 分钟，耗时可能随资源变化。进度可看终端阶段输出与 `runs/<task-id>/status.json`；该文件在阶段切换时更新，**长时间运行的单个 Hadoop 作业内暂不提供实时百分比**。只有状态文件的 `state=completed` 且 `report.json` 存在时才可展示正式结果。`docs/WSL_Hadoop_运行环境.md` 中“原始数据尚未上传”的句子是第二步烟雾测试时的历史状态，第三步实跑结果以本交接文档和正式报告为准。

## 四、接下来怎么做（优先级）

1. **第四步：任务服务与 Agent 接入。** 将脚本封装为可调用工具；一次自然语言请求使用默认登记规则发起完整流水线，并提供“启动任务、查询状态、取得结果、按本次报告追问”的接口。返回任务 ID、阶段、失败原因、版本和实际报告。已有脚本只支持固定 v1 规则，遇到用户要求未实现的自定义规则，应明确拒绝或先设计新版本，不能暗改分数。
2. **服务可靠性。** 在后端处理 Hadoop/WSL 健康检查、服务启动或明确报错、任务并发与超时。当前驱动在作业运行时缓存子进程输出，日志通常在该阶段结束后才写入文件；若前端需要实时进度，应增加 YARN 轮询或流式日志。不要让普通用户手动执行 Hadoop 命令。
3. **第五步：简易前端。** 提供自然语言输入、执行中/完成/失败状态、五维前后分数与变化、逐表行数和处置原因、评分依据与局限、异常样例、净表样例、报告获取及基于报告的追问。所有数字必须来自对应任务的实际 `report.json`，不能写死上一轮结果。
4. **端到端验收与汇报。** 用一次前端提示触发四项真实 Hadoop 作业，核对各 Job 成功标记、三表行数守恒、净表 SHA-256、时间三期合计，并测试 HDFS 不可用/输入校验失败/规则版本不符时的错误展示。准备约 10 分钟的第一次汇报；PPT 或录屏可选，展示版本、真实结果和未解决问题。

建议先阅读项目总要求和迭代一要求，再读规则文档、环境文档、实跑报告，最后看驱动脚本与 Hadoop 任务源码。若计划开始第二轮，先确认拿到**完整净表**并严格按本任务版本及 `T1/T2` 切分，不能从验证期或测试期构造训练输入。
