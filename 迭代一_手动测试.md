# 迭代一手动测试

## 1. 启动

在项目根目录的 Windows PowerShell 中运行：

```powershell
python -m service.app
```

浏览器打开 <http://127.0.0.1:8765>。默认仅监听本机回环地址，不需要安装第三方 Python 包。Windows 服务调用默认 WSL 发行版；可用 `ML1M_WSL_DISTRO` 指定别名。发行版名称可能仍显示 `Ubuntu-22.04`，但系统内部已升级为 Ubuntu 24.04，以 `/etc/os-release` 为准。WSL 内须有 Hadoop 3.5.0、Java 17、可免交互执行的 `sudo -n systemd-run`，且配置目录默认位于 `/home/linxinan/ml1m-hadoop-conf`。服务会检查并尝试启动四个 Hadoop 服务。首次运行前请核对 `docs/WSL_Hadoop_运行环境.md`，**不要重新格式化 NameNode**。

当前这台机器的 WSL 用户是 `awencat`；交接说明描述的是原运行机器的 `linxinan` 环境。新机器可在 WSL 中执行 `bash /mnt/d/aaaeee/ziliao/大数据/文本2/BigData-lab2/scripts/setup_wsl_runtime.sh`：它会从 Apache 与 Eclipse Adoptium 官方源下载并校验 Hadoop 3.5.0 和 Java 17，在当前用户的 `~/.local` 下复制配置，并且**仅当 NameNode 目录全新且为空时**初始化 HDFS。已有 NameNode 数据绝不可重新格式化。驱动会自动查找这套用户目录运行时。历史报告浏览和追问不依赖运行时。

如果当前项目在 WSL 中有固定链接，可设置 `ML1M_WSL_PROJECT` 为该链接路径；否则服务将使用当前盘符的 `/mnt/<盘符>/...` 路径。Hadoop 路径、配置和 HDFS 根目录可分别通过 `ML1M_HADOOP_HOME`、`ML1M_HADOOP_CONF_DIR`、`ML1M_HDFS_ROOT` 覆盖。为排查服务启动问题，可设 `ML1M_AUTO_START_HADOOP=0`，此时要求 Hadoop 服务已经运行。

## 2. 先查看已有正式报告

1. 在“已有任务”选择 `20260923T144724Z-3509cc23`。
2. 预期状态“已完成”，五维分数、三表行数、T1/T2、规则和净表版本来自该任务的 `report.json`。例如 Accurate 为 92.66 → 97.19；Complete 为 99.11 → 98.89。
3. 点击“下载本次报告”，核对下载 JSON 的 `task_id` 与页面一致。
4. 在追问框输入“为什么完整性下降？”，回答应引用本次分数与属性置空。输入“明天票房多少？”，应明确无法从报告回答。
5. 旧任务的 `cleaned.tsv` 不在仓库中时，页面应明确显示净表样例不可用，不能出现编造样例。

这一步只验证已保存的历史报告展示，不证明本机 Hadoop 当前可运行。

## 3. 发起新的端到端任务

1. 使用默认输入：“请使用默认规则清洗 MovieLens 1M，评估清洗前后的五维数据质量，并说明异常处置和局限。”点击“开始清洗与评估”。
2. 页面应立即显示新任务 ID 和“执行中”；刷新页面后，在“已有任务”重新选择该任务，仍可看到阶段。任务期间再次提交应收到并发任务提示。
3. 完成后状态为“已完成”，页面才显示评分。到 `runs/<新任务 ID>/` 检查 `status.json`、`report.json`、`cleaned.tsv`、`clean_process.tsv` 和两份评分 TSV；`runs/<新任务 ID>.service.log` 是服务启动与驱动标准输出日志。
4. 核对报告 `jobs` 含 `score_before`、`clean`、`export_clean`、`score_after` 四个 Job ID；每表 `input = contributing + duplicate + quarantine`；三期数相加等于净表 ratings 行数；本地 `cleaned.tsv` 的 SHA-256 等于报告 `clean_data_sha256`。
5. 页面净表样例应来自新任务的 `cleaned.tsv`，可追问“隔离了多少条？”和“T1/T2 是什么？”。

完整任务在交接环境中约需 9 分钟，实际耗时取决于 WSL 和 Hadoop 资源。运行中阶段更新来自 `status.json`，单个 Hadoop 作业内不提供百分比。

## 4. 失败与规则保护

1. 输入“把评分上限改成 10 再清洗”，应在提交时拒绝，提示仅支持 `quality-clean-v1.0.0`，不创建任务。
2. 输入与数据治理无关的请求，应说明当前支持的任务范围。
3. 若 WSL、HDFS 或 YARN 不可用，新任务应变为“失败”，显示阶段与原因；报告和评分区不应出现占位结果。查看 `runs/<任务 ID>.service.log` 及 `status.json` 排查。
4. 若原始 `.dat` 被更改或规则登记的原始版本不一致，驱动应拒绝；手测时**不要修改正式原始数据**，可在独立副本中验证这一分支。

## 5. 自动测试

```powershell
python -m unittest discover -s tests -v
```

自动测试覆盖既有清洗规则语义、原始文件哈希、请求识别、报告追问、历史报告读取及 HTTP 拒绝自定义规则。迁移到其他机器后，仍需按第 3 节重新实测 Hadoop 四项作业。

## 6. 本机验收记录（2026-09-24）

本次已从前端提交真实任务 `20260924T130302Z-f2c0a4e3`，`status.json` 为 `completed`。四个 Job 分别为 `job_1790253639303_0001` 至 `0004`；页面展示了五维分数、审计样例、净表样例与基于本次报告的追问。净表版本为 `clean-c91a5e196ca1af48c3da`，本地 `cleaned.tsv` 的 SHA-256 与报告完全一致。三表输入行均与“贡献 + 去重 + 隔离”相等；训练、验证、测试合计 `996,113` 条，与净表评分行数一致。

在当前 Windows 检出中，Git 曾自动把 `.dat` 的 LF 转成 CRLF，导致输入校验失败；已添加 `.gitattributes` 锁定原始文件字节，并增加清单哈希测试。另一失败任务验证了 WSL 启动命令错误时页面不会展示占位分数。两次失败任务保留在本机 `runs/` 供排查，正式结果以上述完成任务为准。



注：配置API：$env:OPENAI_BASE_URL = "https://api.deepseek.com/v1"

$env:OPENAI_MODEL = "deepseek-flash v4"

$env:OPENAI_API_KEY = “你的key”
